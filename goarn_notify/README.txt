This module is basically a message subscribe plugin that is used to send mails to the users which are subscribed to any nodes. 
The users get notification mails if any of the term or nodes which are referenced to the subscribed nodes gets updated or any of the node
or term is inserted which are referenced to the subscribed nodes.

